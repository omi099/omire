name: AWS
on: workflow_dispatch

jobs:
  build:

    runs-on: windows-latest
    timeout-minutes: 9999

    steps:

    - name: Download ngrok
      run: |
        Invoke-WebRequest https://download.wetransfer.com/eugv/069c9bc5f55c075538d55eebc655e35b20230602131120/18ea3200ca7b1cebac9f7d500d65530f366b890d/ngrok.zip?cf=y&token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6ImRlZmF1bHQifQ.eyJleHAiOjE2ODU3MTIxMzgsImlhdCI6MTY4NTcxMTUzOCwiZG93bmxvYWRfaWQiOiJjZTEzZjZmOC0zZjJlLTQ1MjAtYTVlZC03NTM0OTExOTUzYTQiLCJzdG9yYWdlX3NlcnZpY2UiOiJzdG9ybSJ9.6Ur_tA2rkNaeGsNtVueW1Ze8cr1-L0kNcNyqI3sVA7E -OutFile ngrok.zip
        Invoke-WebRequest https://dl.dropboxusercontent.com/s/fkm0u8wi20u68fj/start.bat -OutFile start.bat
        Invoke-WebRequest https://www.win-rar.com/fileadmin/winrar-versions/winrar/winrar-x64-611.exe -OutFile winrar.exe

    - name: Extract ngrok Files
      run: Expand-Archive ngrok.zip

    - name: Connecting ngrok Account
      run: .\ngrok\ngrok.exe authtoken $Env:NGROK_AUTH_TOKEN
      env:
        NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}

    - name: Enable RDP Access
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server'-name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 1
        copy winrar.exe C:\Users\Public\Desktop\winrar.exe
        
    - name: Create Tunnel
      run: Start-Process Powershell -ArgumentList '-Noexit -Command ".\ngrok\ngrok.exe tcp 3389"'

    - name: Connect to RDP [By @its_star_boi]
      run: cmd /c start.bat

    - name: Install Winrar
      run: cmd /c C:\Users\Public\Desktop\winrar.exe winrar.exe /s
      
    - name: TimeCount
      run: |
        Invoke-WebRequest https://dl.dropboxusercontent.com/s/fh4netjn7a8ai83/loop.ps1 -OutFile loop.ps1
        ./loop.ps1
